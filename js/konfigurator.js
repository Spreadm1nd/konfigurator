$(document).ready(function() {
					/*Austausch der Imagebilder*/
					
					
					
					/*Variablen definieren */
					
					/* 
					var eiche
					var obj = $("img", "#konfigurator");

					
					obj.click(function(){
							var pos = $(this).attr("src");
							var posTarget = $(this);
							var baseImg = $(obj.get(0) );
							var temp = baseImg.attr("src");


							baseImg.fadeOut('slow', function() {
								baseImg.attr("src", pos)
								.fadeIn('slow');
							});
						
							posTarget.fadeOut('slow', function() {
								posTarget.attr("src", temp)
								.fadeIn('slow');
							});
						
					});



}); // document.ready schließen

				
			